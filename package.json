{
  "name": "value-story",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "bin": { "vs": "./bin/vs.mjs" },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "build:validators": "node scripts/generate-validators.mjs",
    "build:adapters": "node scripts/generate-adapters.mjs",
    "build:gallery": "node scripts/build-gallery.mjs",
    "visual-check": "node ./bin/vs.mjs visual-check"
  },
  "dependencies": {},
  "devDependencies": {
    "ajv": "^8.20.0",
    "playwright": "^1.62.1"
  }
}
